// thumbnail image for each blog, and then pull all the images when the blog is loaded
    // higher resolution?

import React, { useState } from 'react';



export default function BlogCarousel() {
  const [activeCard, setActiveCard] = useState(0);


  return (
    <div>
        <h1>Hey</h1>
    </div>
  );
}
